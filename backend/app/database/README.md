# Database Package (`app/database`)

## Purpose
Manages database infrastructure setup including:
- Connection engines and connection pooling
- Async database session generators (`get_db`)
- Alembic migration support scripts
