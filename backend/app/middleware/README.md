# Middleware Package (`app/middleware`)

## Purpose
Provides custom FastAPI ASGI middlewares for request/response interception, CORS headers, execution timing, rate limiting, and structured HTTP access logging.
