# Models Package (`app/models`)

## Purpose
Contains database ORM entity definitions (e.g., SQLAlchemy/SQLModel classes) representing persistent domain models such as Patients, Scans, Diagnostic Reports, and Audit Logs.
